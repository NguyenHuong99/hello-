#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char line[80];
	printf("Nhap vao ky tu: ", line);
scanf("%[\nABCDEFGHIJKLMEDFSDFDSDSFSDS]", line);
	fflush(stdin);
	printf("Nhap vao ky tu: ", line);
scanf("%[^\n]", line);

	return 0;
}
