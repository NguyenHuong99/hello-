#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
int i;
float x;
char c;
printf("Please enter\n");
scanf("%3d %5f %c", &i, &x, &c);
printf("\nThe values accepted are: %d, %f, %c,", i, x, c);




	return 0;
}
